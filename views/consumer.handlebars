<section class="container">
  <h3 class="consumer-name">{{consumer.consumer_name}}</h3>
  <h4>consumer_goods</h4>

  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each consumer.consumers as |consumer|}}
    {{> consumers-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}