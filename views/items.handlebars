<section class="container">
  <h3 class="items-name">{{items.items_name}}</h3>
  <h4>item_description</h4>
  <h4>item_price</h4>


  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each item.items_name as |item|}}
    {{> items-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}